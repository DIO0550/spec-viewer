# ユーザーレビュー指示

このフォルダは spec-reviewer が作成したユーザーレビューです。

- `manifest.json` の `sourceFiles` にある元の Markdown ファイルを修正してください。
- `context/` 配下は参照用スナップショットなので編集しないでください。
- 対応後は `result.md` に結果を書き、可能なら `status.json` の `status` を `completed` に更新してください。
