# P5.2 Apply With AI Placeholder

## Tasks

- [x] Add disabled or experimental action placement.
- [x] Define selected-comments input model.
- [x] Define generated diff preview model.
- [x] Require explicit user confirmation before file writes.
- [x] Keep Markdown write support out of default comment flow.

## Completion Note

Added a disabled Apply AI placeholder beside the prompt export controls, with frontend-only selected comment input and generated diff preview models. The placeholder explains that provider integration and generated diff previews are required before enabling Markdown writes, and the model keeps explicit user confirmation mandatory. Commit hash is recorded in the final task handoff after commit creation.
