# P2.4 JSON Persistence Format

## Tasks

- [x] Define persistence DTOs for comment JSON.
- [x] Choose JSON array or wrapper object and document the choice.
- [x] Preserve stable field names: `id`, `anchor`, `body`, `resolved`, `createdAt`, `updatedAt`.
- [x] Decide whether timestamps are generated in Rust only.
- [x] Add serde round-trip tests.
- [x] Add malformed JSON tests.

## Done When

- The comment JSON format is stable enough for persisted user data.

## Completion Note

Implemented the infrastructure-only comment JSON format with a canonical versioned wrapper, legacy array input support, domain conversion helpers, validation errors, and round-trip/default/malformed payload tests. Timestamps are persisted by the format but generated by Rust application/domain code before conversion. Verified with `cargo fmt` and `cargo test`.
Commit: 767f86139018196db7a6e096e428361ee4c9871b
