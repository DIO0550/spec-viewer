# P5.1 LLM Prompt Export

## Tasks

- [x] Define prompt bundle format.
- [x] Include selected Markdown context.
- [x] Include unresolved comments.
- [x] Include orphaned comments separately.
- [x] Include workspace/spec/file metadata.
- [x] Add copy-to-clipboard action.

## Completion Note

Implemented LLM prompt generation as a separate contextual export path from raw comment export, with File/Spec/Workspace clipboard actions and coverage for prompt rendering, IPC wiring, and sidebar controls. Commit hash is recorded in the final task handoff after commit creation.
